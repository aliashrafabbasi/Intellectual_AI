# Copy this file to ".env" and fill in values:
#   cp .env.example .env
#
# Required for the chat API (Groq): get a key at https://console.groq.com/
GROQ_API_KEY=

# Optional: model id (default: llama-3.1-8b-instant)
# GROQ_MODEL=llama-3.1-8b-instant
# GROQ_MAX_TOKENS=4096

# MongoDB — chat history. Default matches a local MongoDB on the default port.
# Install MongoDB locally, or use Docker, e.g.:
#   docker run -d --name mongo -p 27017:27017 mongo:7
MONGO_URI=mongodb://127.0.0.1:27017

# Streamlit UI only: URL of this FastAPI app (change if the API runs elsewhere)
INTELLECTUAL_API_URL=http://127.0.0.1:8000
